machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t dataduke/sample-node .

test:
  override:
    - docker run dataduke/sample-node npm test

# Add deployment to production from master branch
# Login to Docker Hub
# Use deploy script
